[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "sharek-agents"
version = "0.1.0"
description = "SHARE-K AI agents service"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
